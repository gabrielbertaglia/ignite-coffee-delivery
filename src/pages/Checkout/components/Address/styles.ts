import styled from 'styled-components'
import { BaseCardCheckout } from '../../styles'

export const ContainerAddress = styled(BaseCardCheckout)``

export const AddressHeader = styled.div`
  display: flex;
  gap: 0.5rem;

  padding-bottom: 2rem;
`
